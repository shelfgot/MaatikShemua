# Core backend dependencies for development and tests.
# This set is intentionally compatible with Python 3.14 and
# does NOT include heavy OCR/HTR dependencies like kraken.
# For running kraken-based pipelines, install the optional
# requirements file documented in the README.

# FastAPI and server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
websockets>=12.0

# Database
sqlalchemy>=2.0.25
alembic>=1.13.1

# Image processing
pillow>=10.2.0
pyvips>=2.2.2
PyMuPDF>=1.23.0

# XML processing
lxml>=5.1.0

# IIIF
iiif-prezi3>=1.1.0

# Background tasks (optional)
celery>=5.3.0
redis>=5.0.0

# Utilities
tenacity>=8.2.0
structlog>=24.1.0
python-dotenv>=1.0.0
packaging>=23.2

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.26.0
